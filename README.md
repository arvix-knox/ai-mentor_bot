# 🤖 AI Mentor Bot

Telegram-бот с WebApp-интерфейсом и backend API на FastAPI.  
Архитектура: **Telegram Bot + FastAPI + WebApp + PostgreSQL**.

Проект подходит как база для AI-наставника, SaaS-продукта или образовательной платформы.

---

# 📌 Возможности

- 🤖 Telegram-бот
- 🌐 Telegram Web App (встроенный frontend)
- ⚡ Backend на FastAPI
- 🗄 PostgreSQL
- 🔁 Автоматическое переподключение к Telegram
- 🧪 Поддержка тестов
- 🧩 Разделённая архитектура (бот / API / WebApp)

---

# 🏗 Архитектура проекта

```
ai-mentor_bot/
│
├── src/
│   ├── api/              # FastAPI backend
│   ├── bot/              # Telegram bot логика
│   ├── webapp/           # WebApp (HTML/JS)
│   └── main.py           # Точка входа
│
├── tests/                # Тесты
├── alembic/              # Миграции БД
├── requirements.txt
├── .env.example
└── Makefile
```

---

# 🚀 Быстрый старт

## 1️⃣ Клонирование

```bash
git clone https://github.com/arvix-knox/ai-mentor_bot.git
cd ai-mentor_bot
```

---

## 2️⃣ Создание виртуального окружения

```bash
python3 -m venv venv
source venv/bin/activate
```

---

## 3️⃣ Установка зависимостей

```bash
pip install -r requirements.txt
```

---

## 4️⃣ Настройка переменных окружения

Создай файл `.env`:

```env
BOT_TOKEN=your_telegram_bot_token
DATABASE_URL=postgresql://user:password@localhost:5432/mentor_db
WEBAPP_URL=http://127.0.0.1:8000/webapp
```

---

# 🗄 Настройка базы данных

1. Установи PostgreSQL  
2. Создай базу:

```sql
CREATE DATABASE mentor_db;
```

3. Примени миграции:

```bash
alembic upgrade head
```

---

# ⚡ Запуск проекта

## ▶ Запуск API (FastAPI)

```bash
uvicorn src.api.app:app --host 127.0.0.1 --port 8000 --reload
```

API будет доступен по адресу:

```
http://127.0.0.1:8000
```

Swagger-документация:

```
http://127.0.0.1:8000/docs
```

---

## 🌐 WebApp

После запуска API:

```
http://127.0.0.1:8000/webapp
```

---

## 🤖 Запуск Telegram бота

В отдельном терминале:

```bash
python -m src.main
```

---

# 🧪 Тестирование

```bash
pytest
```

---

# 📦 Переменные окружения

| Переменная    | Описание                        |
|--------------|---------------------------------|
| BOT_TOKEN     | Токен Telegram бота             |
| DATABASE_URL  | Строка подключения к PostgreSQL |
| WEBAPP_URL    | URL WebApp (production режим)   |

---

# 🐳 Docker (рекомендуется)

Можно добавить:

- Dockerfile
- docker-compose.yml
- PostgreSQL контейнер
- Nginx reverse proxy

Пример сервисов:

```
api
bot
postgres
nginx
```

---

# 🚀 Деплой

Подходит для:

- VPS (Ubuntu)
- Railway
- Render
- Fly.io
- Любой Docker-хостинг

Рекомендуется:

- Использовать HTTPS
- Настроить reverse proxy
- Хранить секреты через переменные окружения

---

# 🔮 Возможные улучшения

- Авторизация пользователей
- История диалогов
- Интеграция AI моделей
- Кэширование через Redis
- Event-driven архитектура (RabbitMQ / Kafka)
- CI/CD через GitHub Actions
- Docker + автоматический деплой

---

# 📄 Лицензия

Добавьте LICENSE файл при необходимости.

---

# 🧠 Итог

Проект представляет собой модульную Telegram-систему с WebApp и API backend.  
Подходит как основа для AI-ассистента, образовательного бота или SaaS-продукта.

---

Если проект оказался полезным — ⭐ поставь звезду на GitHub.
